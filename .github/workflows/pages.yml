name: Deploy Ouroboros Demo to GitHub Pages

on:
  push:
    branches: [main]
    paths:
      - "docs/site/**"
      - "src/ouroboros/**"
      - ".github/workflows/pages.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Pages
        uses: actions/configure-pages@v5
      - name: Build
        # The demo is fully static (Pyodide loads the Python source from the
        # raw GitHub branch at runtime), so "build" is just copying the site.
        run: |
          mkdir -p _site
          cp -r docs/site/* _site/
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
